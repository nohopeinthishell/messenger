<section class="card {{class}}">
  <h1 class="card__title">{{title}}</h1>
  {{{FormAuth
    inputs=inputs
    button=button
    links=links
    formType=formType
    class="card__form"
    contentClass="card__content"
    actionsClass="card__btns"
  }}}
</section>
